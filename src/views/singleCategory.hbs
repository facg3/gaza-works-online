<div class="jobt">
  {{#each project}}
  <h2>{{title}}</h2>
  {{/each}}
  <div>
    <h3>{{reqCategory}}</h3>
    {{#each project}}
    <h3>{{skills}}</h3>
    <h4>{{budget}}, {{lifetime}}</h4>
    <p>{{description}}</p>
  </div>
  {{/each}}
</div>
